apiVersion: v1
kind: Service

metadata:
  name: vote
  namespace: vote
  labels:
    name: voting-service
    app: voting-app

spec:
  selector:
    name: voting-app-pod
    app: voting-app

  type: NodePort

  ports:
    - nodePort: 31000
      port: 8080
      targetPort: 80
